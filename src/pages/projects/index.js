import React from 'react';
import Layout from '../../components/layout';

const ProjectPage = () => {
	return (
		<Layout title="hello">
			<p>Projects</p>
		</Layout>
	);
};

export default ProjectPage;
